import 'package:flutter/material.dart';

class AppColors {
  // General
  static const Color background = Color(0xFF212121);
  static const Color accent = Color(0xFFFFC107);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // BottomNavigationBar
  static const Color unselectedItemColor = Color(0xFF808187);

  // TextFormField
  static const Color textFieldBackground = Color(0xFF20232C);
  static const Color textFieldTextDisable = Color(0xFF808187);
}
